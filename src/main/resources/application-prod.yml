# Coupon/Eureka Server Port (prod) : "배포설명" 확인

# Coupon Server Port
server:
  port: ${PORT_NUMBER}

# Eureka Server URL
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL_PROD} # ci-cd.yml 환경변수 추가 필요

logging:
  level:
    root: info